import api from "./api";

// MarketplaceController in the supplied backend currently has no mapped endpoints.
// These methods are intentionally not used until backend marketplace endpoints exist.
export const marketplaceUnavailable = true;
export default api;
