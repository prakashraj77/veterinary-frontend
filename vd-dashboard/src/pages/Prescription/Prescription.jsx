import { useEffect, useState } from "react";
import toast from "react-hot-toast";
import { FiMic, FiTrash2, FiDownload, FiSend, FiMail, FiMessageSquare } from "react-icons/fi";
import { FaWhatsapp } from "react-icons/fa";
import Input, { Field, Select, Textarea } from "../../components/ui/Input";
import Button from "../../components/ui/Button";
import { getPatients } from "../../services/patientService";
import { getMedicines } from "../../services/medicineService";
import { createPrescription } from "../../services/prescriptionService";
import { sendSms, sendWhatsApp, sendEmail, generatePdf } from "../../services/communicationService";
import "./Prescription.css";

const emptyMed = () => ({ id: Date.now() + Math.random(), medicineId: "", dosage: "", frequency: "Once daily", duration: "", route: "", quantity: "", instructions: "" });
export default function Prescription() {
  const [patients,setPatients]=useState([]);const[allMeds,setAllMeds]=useState([]);const[patientId,setPatientId]=useState("");const[diagnosis,setDiagnosis]=useState("");const[instructions,setInstructions]=useState("");const[notes,setNotes]=useState("");const[medicines,setMedicines]=useState([emptyMed()]);const[visitDate,setVisitDate]=useState(new Date().toISOString().slice(0,10));const[complaint,setComplaint]=useState("");const[vitals,setVitals]=useState({temp:"",pulse:"",resp:""});const[saving,setSaving]=useState(false);
  useEffect(()=>{Promise.all([getPatients(),getMedicines()]).then(([p,m])=>{setPatients(Array.isArray(p)?p:[]);setAllMeds(Array.isArray(m)?m:[]);}).catch(()=>{});},[]);
  const patient=patients.find(p=>String(p.id)===String(patientId));
  const updateMed=(id,key,value)=>setMedicines(ms=>ms.map(m=>m.id===id?{...m,[key]:value}:m));
  const submit=async()=>{if(!patientId)return toast.error("Select a patient");const items=medicines.filter(m=>m.medicineId).map(m=>({medicineId:Number(m.medicineId),dosage:m.dosage,frequency:m.frequency,duration:m.duration,route:m.route,quantity:m.quantity,instructions:m.instructions}));try{setSaving(true);const rx=await createPrescription({patientId:Number(patientId),prescriptionDate:visitDate,diagnosis,instructions,notes,doctorName:"Veterinary Doctor",items});toast.success(`Prescription #${rx.id} saved`);return rx;}catch(e){toast.error(e?.response?.data?.message||"Could not save prescription");throw e;}finally{setSaving(false);}};
  const content=()=>`Patient: ${patient?.name||""}\nDiagnosis: ${diagnosis}\nComplaint: ${complaint}\nMedicines:\n${medicines.map(m=>{const med=allMeds.find(x=>String(x.id)===String(m.medicineId));return `${med?.name||""} ${m.dosage} ${m.frequency} ${m.duration}`;}).join("\n")}\nInstructions: ${instructions}\nNotes: ${notes}`;
  const downloadPdf=async()=>{try{const blob=await generatePdf("Veterinary Prescription",content());const url=URL.createObjectURL(blob);const a=document.createElement("a");a.href=url;a.download="zenve-prescription.pdf";a.click();URL.revokeObjectURL(url);toast.success("PDF generated");}catch(e){toast.error("Could not generate PDF");}};
  const send=async(type)=>{try{await submit();const message=content();if(type==="whatsapp")await sendWhatsApp({phoneNumber:patient?.ownerPhone||"",message,type:"PRESCRIPTION",provider:"MANUAL"});if(type==="sms")await sendSms({phoneNumber:patient?.ownerPhone||"",message,type:"PRESCRIPTION",provider:"MANUAL"});if(type==="email")await sendEmail({recipient:patient?.ownerEmail||"",subject:"Veterinary prescription",message,type:"PRESCRIPTION",provider:"MANUAL"});toast.success(`${type} sent`);}catch(e){}}
  return <div className="rx-layout"><div className="panel stack-4"><div className="rx-grid-3"><Field label="Patient" className="col-span-2"><Select value={patientId} onChange={e=>setPatientId(e.target.value)}><option value="">Select patient</option>{patients.map(p=><option key={p.id} value={p.id}>{p.name} · {p.species} · {p.ownerName||""}</option>)}</Select></Field><Field label="Weight"><Input value={patient?.weight!=null?`${patient.weight} kg`:""} readOnly /></Field></div><div className="rx-mic-box"><button onClick={()=>toast("Voice capture needs a speech service connection")} className="rx-mic-btn"><FiMic size={16}/></button><div><p className="rx-mic-title">Voice → Prescription</p><p className="rx-mic-desc">Voice capture is ready for a speech provider; the saved prescription still uses the backend API.</p></div></div><Field label="Date"><Input type="date" value={visitDate} onChange={e=>setVisitDate(e.target.value)}/></Field><Field label="Presenting complaint"><Input value={complaint} onChange={e=>setComplaint(e.target.value)}/></Field><Field label="Diagnosis"><Input value={diagnosis} onChange={e=>setDiagnosis(e.target.value)}/></Field><div><span className="eyebrow">Medicines</span><div className="stack-3" style={{marginTop:12}}>{medicines.map(m=><div key={m.id} className="rx-med-row"><Select value={m.medicineId} onChange={e=>updateMed(m.id,"medicineId",e.target.value)}><option value="">Medicine</option>{allMeds.map(x=><option key={x.id} value={x.id}>{x.name} {x.strength||""}</option>)}</Select><Input placeholder="Dose" value={m.dosage} onChange={e=>updateMed(m.id,"dosage",e.target.value)}/><Input placeholder="Frequency" value={m.frequency} onChange={e=>updateMed(m.id,"frequency",e.target.value)}/><Input placeholder="Duration" value={m.duration} onChange={e=>updateMed(m.id,"duration",e.target.value)}/><button onClick={()=>setMedicines(ms=>ms.filter(x=>x.id!==m.id))} className="rx-med-remove"><FiTrash2 size={16}/></button></div>)}<button onClick={()=>setMedicines(ms=>[...ms,emptyMed()])} className="rx-chip">+ Add medicine</button></div></div><div className="rx-grid-4"><Field label="Temp (°C)"><Input value={vitals.temp} onChange={e=>setVitals({...vitals,temp:e.target.value})}/></Field><Field label="Pulse"><Input value={vitals.pulse} onChange={e=>setVitals({...vitals,pulse:e.target.value})}/></Field><Field label="Resp"><Input value={vitals.resp} onChange={e=>setVitals({...vitals,resp:e.target.value})}/></Field><Field label="Follow-up days"><Input placeholder="10"/></Field></div><Field label="Instructions"><Input value={instructions} onChange={e=>setInstructions(e.target.value)}/></Field><Field label="Notes"><Textarea rows={3} value={notes} onChange={e=>setNotes(e.target.value)}/></Field><div className="rx-footer"><div className="flex-row" style={{gap:8}}><Button variant="secondary" icon={FiDownload} onClick={downloadPdf}>PDF</Button><Button variant="secondary" icon={FaWhatsapp} onClick={()=>send("whatsapp")}>WhatsApp</Button><Button variant="secondary" icon={FiMail} onClick={()=>send("email")}>Email</Button><Button variant="secondary" icon={FiMessageSquare} onClick={()=>send("sms")}>SMS</Button></div><Button icon={FiSend} onClick={submit} disabled={saving}>{saving?"Saving...":"Save prescription"}</Button></div></div><div className="panel rx-preview"><div className="rx-preview-top"><div><p className="rx-preview-name">Zenve Veterinary</p><p className="rx-preview-faint">Veterinary Doctor</p></div><div style={{textAlign:"right"}}><p className="rx-preview-name">Prescription</p><p className="rx-preview-faint">{visitDate}</p></div></div><div className="rx-preview-grid"><div><p className="eyebrow">Patient</p><p className="rx-preview-value">{patient?.name||"—"} · {patient?.species||""} · {patient?.breed||""}</p></div><div><p className="eyebrow">Owner</p><p className="rx-preview-value">{patient?.ownerName||"—"}</p></div><div><p className="eyebrow">Weight</p><p className="rx-preview-value">{patient?.weight!=null?`${patient.weight} kg`:"—"}</p></div><div><p className="eyebrow">Date</p><p className="rx-preview-value">{visitDate}</p></div></div><div className="rx-preview-block"><p className="eyebrow">Complaint</p><p className="rx-preview-value">{complaint||"—"}</p></div><div className="rx-preview-block"><p className="eyebrow">Diagnosis</p><p className="rx-preview-value">{diagnosis||"—"}</p></div><div className="rx-preview-block"><p className="eyebrow">℞ Medicines</p>{medicines.map((m,i)=>{const med=allMeds.find(x=>String(x.id)===String(m.medicineId));return <div key={m.id} style={{marginBottom:6}}><p className="cell-title">{i+1}. {med?.name||"—"}</p><p className="cell-sub">{[m.dosage,m.frequency,m.duration].filter(Boolean).join(" · ")||"—"}</p></div>})}</div><div className="rx-preview-block"><p className="eyebrow">Instructions</p><p className="rx-preview-value">{instructions||"—"}</p></div><div className="rx-preview-block"><p className="eyebrow">Notes</p><p className="rx-preview-value">{notes||"—"}</p></div></div></div>;
}
